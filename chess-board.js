let size = 8;


for (let outerLoop = 0; outerLoop < size; outerLoop++) {
    for (let innerloop = 0; innerloop <= size; innerloop++) {
        if (size % 2 == 0) {
            console.log("#");
            // console.log("\n");
        }
    }
}