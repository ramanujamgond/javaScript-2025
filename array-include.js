// array include
const names = ['Florin', 'Ivan', 'Liam'];
const res = names.includes('Ivan');
console.log(res);

const fruits = ['Apple', 'Mango', 'Orange'];
const results = fruits.includes('Orange');

if (results) {
    console.log("Yuppy!");
} else {
    console.log("Sad... Let's buy an ornage");
}

