//write a small snippet that

/*
the width represent the width of the rectange in cm
and the length represent the length of the rectane in cm
*/
let width = 34;
let length = 20;

// the formula to calculate the width of the rectange is w * l;
console.log('Area of the rectange is', width * length);