// array unshift
// add the items at the start of the array either one item can be added or more can be added

const numbers = [1, 2, 3, 4, 5];
// const results = numbers.unshift(0);
const results = numbers.unshift(-1, 0);
// returns the length of array
console.log(results);
console.log(numbers);