// array shift
// this methods remove the item from the start of the array and returns the remove items
const numbers = [1, 2, 3, 4];
const res = numbers.shift();

console.log(res);
console.log(numbers);