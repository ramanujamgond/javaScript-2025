// multiplication table for a given number

let number = 9;
let multiplicationValue = 1;

while (multiplicationValue <= 10) {
    console.log(`${number} x ${multiplicationValue} = `, number * multiplicationValue);
    multiplicationValue++;
}