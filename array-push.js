// array push
const numbers = [1, 2, 3, 4];

// one number can be pushed
// push mentod also return the pushed item
const num = numbers.push(5);
console.log(num);

// or multiple numbers can be pushed
numbers.push(6, 7, 8);

console.log(numbers);